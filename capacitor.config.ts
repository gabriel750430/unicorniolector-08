import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unicorniolector.app',
  appName: 'Unicorniolector',
  webDir: 'dist'
};

export default config;
